// Only apply aggressive 1-second monitoring to specific integration tools

process {

    executor {
        jobResources = [
            memory: '1s',
            cpu: '1s'
        ]
    }

    withName: 'SCVITOOLS_SCVI' {
        // Inherits the executor settings defined above: memory: '1s', cpu: '1s'
    }

    withName: 'SCANPY_HARMONY' {
        // Inherits the executor settings
    }

    withName: 'INTEGRATION_BBKNN' {
        // Inherits the executor settings
    }

    withName: 'SCANPY_COMBAT' {
        // Inherits the executor settings
    }
}